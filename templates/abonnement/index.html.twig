{% extends 'base2.html.twig' %}


{% block body %}
<main id="main" class="main">



        <div class="pagetitle">

        </div><!-- End Page Title -->

         <section class="section">
            <div class="row">
                <div class="col-lg-12">

                    <div class="card">

                        {% for message in app.flashes('notice') %}
                            <div class="alert alert-success bg-success text-light border-0 alert-dismissible fade show" role="alert">
                                {{ message }}
                            </div>

                        {% endfor %}


                        {% for message in app.flashes('noticedelete') %}
                            <div class="alert alert-danger bg-danger text-light border-0 alert-dismissible fade show" role="alert">
                                {{ message }}
                            </div>

                        {% endfor %}

                        <div class="card-body">

                            <h5 class="card-title">Membership  lists</h5>



<!-- Default Table -->
    <table class="table">
        <thead>
            <tr>
               
                <th>Type</th>
                <th>Prix</th>
                <th>Dateachat</th>
                <th>Dateexpiration</th>
                
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for abonnement in abonnements %}
            <tr>
               

                <td>{{ abonnement.getType() }}</td>
                <td>{{ abonnement.getPrix() }}</td>
                <td>{{ abonnement.getDateachat() ? abonnement.getDateachat()|date('Y-m-d') : '' }}</td>
                <td>{{ abonnement.getDateexpiration() ? abonnement.getDateexpiration()|date('Y-m-d') : '' }}</td>
               
                <td>
                    <a href="{{ path('app_abonnement_show', {'ida': abonnement.ida}) }}">show</a>
                    <a href="{{ path('app_abonnement_edit', {'ida': abonnement.ida}) }}">edit</a>
                </td>

            </tr>
        
        {% endfor %}
                    <a href="{{ path('app_abonnement_new') }}">Create new</a>

        </tbody>
    </table>

         <!-- End Default Table Example -->

                        </div>

                    </div>



                </div>


            </div>
        </section>

    </main>

{% endblock %}