<form method="post" action="{{ path('app_client_delete', {'id_client': client.idClient}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.idClient) }}">
    <button class="btn">Delete account</button>
</form>
