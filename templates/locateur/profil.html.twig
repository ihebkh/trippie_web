{% extends 'base.html.twig' %}


{% block header %}


 <header id="header" class="fixed-top d-flex align-items-center">
    <div class="container d-flex align-items-center justify-content-between">

      <div class="logo">
        <h1><a href="{{path('profilloc', {'id_loc' : locateur.idLoc})}}">Tripee</a></h1>
        <!-- Uncomment below if you prefer to use an image logo -->
        <!-- <a href="index.html"><img src="assets/img/logo.png" alt="" class="img-fluid"></a>-->
      </div>

      <nav id="navbar" class="navbar">
        <ul>
          <li><a class="nav-link scrollto active" href="{{path('profilloc', {'id_loc' : locateur.idLoc})}}">Home</a></li>
          <li><a class="nav-link scrollto active" href="{{ path('app_voitureaffichenonreserve', {'id_loc' : locateur.idLoc}) }}">Car</a></li>
          <li><a class="nav-link scrollto active" href="{{ path('app_reclamation_frontloc', {'id_loc' : locateur.idLoc }) }}">Reclamation</a></li>
          <li class="dropdown">
      <a class="nav-link scrollto active" href="">  <i class="bi bi-gear" style="font-size: 1.9em;"></i></a>
      <ul>
       <li>
       <a href="{{ path('app_locateur_delete', {'id_loc': locateur.idLoc})}}"></a>
       {{ include('locateur/_delete_form.html.twig') }}
       </li>
       
      </ul>
      </li>
          <li class="scrollto" style="margin-left: 20px" >
      
  
                <img class="" src="{{ asset('uploads/' ~ locateur.img) }}" style="border-radius: 50%;width: 45px; height: 45px;">
                
                    <span style="font-size: 15px;color: #222222;margin-bottom: 2000px;">
                {{ locateur.idRole.idUser.nom }}
                {{ locateur.idRole.idUser.prenom }}
 <a  style="display: inline-block; margin-left: -30px;" class="nav-link scrollto active" href="{{ path('app_locateur_edit', {'id_loc': locateur.idLoc}) }}"><i class="bi bi-pencil-square" style="font-size: 1em;"></i></a>
                </span>
    
        </li>
  <li>
      <a class="nav-link scrollto active" href="{{ path('login') }}">  <i class="bi bi-box-arrow-right" style="font-size: 1.9em;"></i></a>
      </li>
        </ul>
      
       
      </nav><!-- .navbar -->
 
    </div>
    
  </header><!-- End Header -->


{% endblock %}