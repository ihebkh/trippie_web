<form method="post" action="{{ path('app_locateur_delete', {'id_loc': locateur.idLoc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locateur.idLoc) }}">
    <button class="btn">Delete account</button>
</form>
