{% extends 'base2.html.twig' %}

{% block body %}

    <main id="main" class="main">



        <div class="pagetitle">

        </div><!-- End Page Title -->

        <section class="section">
            <div class="row">
                <div class="col-lg-12">

                    <div class="card">

                        <div class="card-body">

                            <h5 class="card-title">Landlords list</h5>


                            <!-- Default Table -->
                            <table class="table">
                                <thead>
                                <tr>

                                    <th>Picture</th>
                                    <th>Cin</th>
                                    <th>Firstname</th>
                                    <th>Lastname</th>
                                    <th>Email</th>
                                    <th>Status</th>
                                </tr>
                                </thead>
                                <tbody>




                                  {%  for locateur in locateurs %} 



                                    <tr>

                                    <td><img class="img-profile " src="{{ asset('uploads/' ~ locateur.img) }}" style="border-radius: 50%;width: 100px; height: 100px;"></td>
                                    <td>{{ locateur.idRole.idUser.cin }}</td>
                                    <td>{{ locateur.idRole.idUser.nom }}</td>
                                    <td>{{ locateur.idRole.idUser.prenom }}</td>
                                    <td>{{ locateur.email }}</td>
                                    <td style="color:{% if locateur.etat == 'enabled' %}green{% else %}red{% endif %};font-weight:bold;">{{ locateur.etat }}</td>
                                    <td>
                                    <button><a href="{{ path('disable_locateur', {'id_loc': locateur.idLoc}) }}" class="btn btn-danger btn-sm">Disable</a></button></a>
                                    </td>

                                    </tr>

                                    {% endfor %}


                                </tbody>
                            </table>
                            <!-- End Default Table Example -->

                        </div>

                    </div>



                </div>


            </div>
        </section>

    </main>

{% endblock %}
