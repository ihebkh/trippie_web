{% extends 'indexPromotion.html.twig' %}

{% block body %}
<br><br><br><br><br><br><br><br><br>

{{ form_start(form) }}
    {{ form_row(form.price) }}
    {{ form_row(form.code_coupon) }}
    {{ form_widget(form.submit) }}
{{ form_end(form) }}  

<script>
    {% if price is defined and newPrice is defined %}
        alert("price : {{price}} \n discount : {{newPrice}}");
    {% endif %}
</script>
 
{% endblock %}
