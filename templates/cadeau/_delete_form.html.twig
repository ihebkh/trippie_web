<form method="post" action="{{ path('app_cadeau_delete', {'idcadeau': cadeau.idcadeau}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cadeau.idcadeau) }}">
    <button class="btn">Delete</button>
</form>
