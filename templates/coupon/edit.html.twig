
{% extends 'base2.html.twig' %}

{% block title %}Edit Coupon{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        .example-wrapper {
   margin: 1em auto;
      max-width: 500px;
      width: 95%;
      font: 18px/1.5 sans-serif;
      text-align: center; /* Add this line to center the content */
}
</style>

<div class="example-wrapper">
    <h1>Edit Coupon</h1>

  {{ include('coupon/_form.html.twig', {'button_label': 'Update', 'attr': {'class': 'btn btn-outline-dark'}}) }}


    <a href="{{ path('app_coupon_index') }}">back to list</a>

    {{ include('coupon/_delete_form.html.twig') }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        function generateCode() {
            var codeCoupon = document.getElementById("coupon1_code_coupon");
            var length = 8;
            var charset = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
            var code = "";
            for (var i = 0, n = charset.length; i < length; ++i) {
                code += charset.charAt(Math.floor(Math.random() * n));
            }
            codeCoupon.value = code;
        }
    </script>
{% endblock %}

