<form method="post" action="{{ path('app_role_delete', {'id_role': role.id_role}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.id_role) }}">
    <button class="btn">Delete</button>
</form>
