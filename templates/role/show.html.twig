{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_role</th>
                <td>{{ role.idRole }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ role.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_role_index') }}">back to list</a>

    <a href="{{ path('app_role_edit', {'id_role': role.id_role}) }}">edit</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
