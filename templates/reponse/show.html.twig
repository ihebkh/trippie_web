{% extends 'base2.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
<main id="main" class="main">
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
            <tr>
                <th>Id reclamation</th>
                <td>{{ reponse.getIdRec() }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}"<button type="button" class="btn btn-secondary">Edit</button></a>

    <a href="{{ path('app_reponse_index') }}"<button type="button" class="btn btn-secondary">Back to list</button></a>
    <br></br>

    {{ include('reponse/_delete_form.html.twig') }}
    </main>
{% endblock %}
