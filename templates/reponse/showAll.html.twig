{% extends "frontbase.html.twig" %}

{% block title %}Reponse index{% endblock %}

{% block body %}
    <h1>Reponse List</h1>

    <table class="table table-hover " style="background-color: white">
        <thead>
        <tr>
            
            <th>Reponse</th>
                <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for reponses in reponses %}
            <tr>
                
                <td>{{ reponses.reponse }}</td>
                <td>{{ reponses.getIdrec() }}</td>

            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <td><a href="{{ path('app_reclamation_front') }}"<button type="button" class="btn btn-secondary">Back to reclamations</button></a></td>


{% endblock %}