{% extends 'base2.html.twig' %}


{% block body %}
    <main id="main" class="main">



        <div class="pagetitle">

        </div><!-- End Page Title -->

         <section class="section">
            <div class="row">
                <div class="col-lg-12">

                    <div class="card">

                        {% for message in app.flashes('notice') %}
                            <div class="alert alert-success bg-success text-light border-0 alert-dismissible fade show" role="alert">
                                {{ message }}
                            </div>

                        {% endfor %}


                        {% for message in app.flashes('noticedelete') %}
                            <div class="alert alert-danger bg-danger text-light border-0 alert-dismissible fade show" role="alert">
                                {{ message }}
                            </div>

                        {% endfor %}

                        <div class="card-body">

                            <h5 class="card-title">Fidelity Card  lists</h5>



<!-- Default Table -->


    <table class="table">
        <thead>
            <tr>
                
                <th>Pointmerci</th>
                <th>Dateexpiration</th>   
              
                <th>show</th>
                <th>edit</th>
             
            </tr>
        </thead>
        <tbody>
        {% for cartefidelite in cartefidelites %}
            <tr>
                
                <td>{{ cartefidelite.getPointmerci }}</td>
                <td>{{ cartefidelite.getDateexpiration ? cartefidelite.dateexpiration|date('Y-m-d') : '' }}</td>
                
                <td>
                <button class="btn btn-outline-dark" >   <a href="{{ path('app_cartefidelite_show', {'idCf': cartefidelite.idCf}) }}">show</a> </button> 
                </td>
                <td>
                 <button  class="btn btn-outline-success">     <a href="{{ path('app_cartefidelite_edit', {'idCf': cartefidelite.idCf}) }}">edit</a></button> 
                    

                </td>


                
            </tr>
        
        {% endfor %}
                <button class="btn btn-outline-info">    <a href="{{ path('app_abonnement_new') }}">Create new</a></button>

        </tbody>
    </table>

{% endblock %}
