{% extends 'base2.html.twig' %}



{% block body %}
   <main id="main" class="main">





        <section class="section">
            <div class="row">
                <div class="col-lg-12">

                    <div class="card">

                        {% for message in app.flashes('notice') %}
                            <div class="alert alert-success bg-success text-light border-0 alert-dismissible fade show" role="alert">
                                {{ message }}
                            </div>

                        {% endfor %}


                        {% for message in app.flashes('noticedelete') %}
                            <div class="alert alert-danger bg-danger text-light border-0 alert-dismissible fade show" role="alert">
                                {{ message }}
                            </div>

                        {% endfor %}

                        <div class="card-body">

                            <h5 class="card-title">List Articles</h5>


                            <!-- Default Table -->

    <table class="table">
        <tbody>
           
            <tr>
                <th>Pointmerci</th>
                <td>{{ cartefidelite.pointmerci }}</td>
            </tr>
            <tr>
                <th>Dateexpiration</th>
                <td>{{ cartefidelite.dateexpiration ? cartefidelite.dateexpiration|date('Y-m-d') : '' }}</td>
            </tr>
              <tr>
                   <td> <a href="{{ path('app_cartefidelite_edit', {'idCf': cartefidelite.idCf}) }}">edit</a></td>

            </tr>

<th>

    <a href="{{ path('app_cartefidelite_index') }}">back to list</a>
</th>



        </tbody>
    </table>



   
{% endblock %}
