<form method="post" action="{{ path('app_chauffeur_delete', {'id_ch': chauffeur.idCh}) }}" onsubmit="return confirm('Are you sure you want to delete your account defintively?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chauffeur.idCh) }}">
    <button class="btn">Delete account</button>
</form>
